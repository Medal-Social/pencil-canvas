---
'@medalsocial/pencil-canvas': patch
---

Add `README.md` with install instructions, quick-start example, full `<PenViewer>` props table, and lower-level `parseNodeTree` / `resolveLayout` usage. The npm package previously shipped without a README, leaving the package page empty after the registry cache expired.
