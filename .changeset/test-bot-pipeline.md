---
'@medalsocial/pencil-canvas': patch
---

Validate the new Release Bot + Medal Approvals pipeline end-to-end (no source/runtime change). Future patches/minors will follow the same hands-off flow: Release Bot opens the release PR, CI fires automatically, Medal Approvals auto-approves, merge to publish.
